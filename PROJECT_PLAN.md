# MobileVision 手机拍照与书写自动传输方案

日期：2026-09-13
状态：Windows 端已实现首个开发预览版；Android 已实现扫码、拍照与自动上传，已通过打包版跨端模拟验证；真机联调尚未完成。详见 PROJECT_STATUS.md。详见 windows/README.md。

## 1. 产品目标与范围

用户在 Android 手机上拍照、导入相册图片或直接用电容笔书写，生成的图片自动传到 Windows 电脑；电脑软件保存并立即展示，方便在大屏上查看和截图。

首版支持一部 Android 手机连接一台 Windows 电脑，两端位于能够互相访问的局域网。电脑可以使用网线连接路由器，手机使用 Wi-Fi。

本轮范围已替代此前的视频方案：不实现实时视频传输、电脑抓帧、录像、电脑遥控快门、虚拟摄像头、云端账号或跨公网连接。

建议支持 Windows 10/11 x64、Android 10 及以上；最低系统版本及依赖版本在开发时通过构建与真机兼容性验证后固定。

## 2. 使用流程

1. 打开电脑软件，选择照片保存目录，软件启动本地接收服务并显示配对二维码。
2. 打开手机 App，授权摄像头，扫码连接电脑；界面显示电脑名称与连接状态。
3. 手机进入拍摄模式拍照或导入相册，也可以进入书写模式完成笔迹后点击“同步到电脑”。
4. App 将 JPEG 照片或 PNG 书写快照保存到自身本地存储，创建持久化待传记录，然后自动上传。
5. 电脑完整接收并保存照片，返回成功确认，界面自动展示最新收到的照片。
6. 手机将照片标记为“已传到电脑”；网络中断时保留待传记录，恢复连接后重试。

首版优先保证 App 前台运行时的传输。退出或被系统终止后，待传记录仍保留，下次打开并连接后继续上传；不承诺后台持续传输。

## 3. 技术架构

```text
Android App                                  Windows 软件
CameraX 相机预览与拍照                       Electron + React 界面
         ↓                                           ↑
本地 JPEG/PNG + 持久化上传队列 ─ HTTPS 上传 ─→ 接收服务
         ↑                                   ↓
         └────────── 保存成功确认 ────── 照片目录 + 本地索引
```

| 模块 | 技术选型 | 职责 |
| --- | --- | --- |
| Android 界面 | Kotlin + Jetpack Compose | 扫码、相机页面、连接及上传状态 |
| Android 相机 | CameraX Preview + ImageCapture | 相机预览、拍摄 JPEG、切换前后摄像头 |
| Android 书写 | Compose Canvas + Android Bitmap | 笔迹编辑、草稿保存、生成无损 PNG 快照 |
| Android 扫码 | 本地二维码识别组件，开发时固定依赖 | 解析电脑配对信息；扫码页与拍照页依次使用摄像头 |
| Android 网络 | OkHttp | HTTPS 配对、上传、超时与取消 |
| Android 队列 | SQLiteOpenHelper + App 私有文件目录 | 保存照片路径、唯一 ID、上传状态和重试信息 |
| Windows 界面 | Electron + TypeScript + React | 配对、大图查看、缩略图列表、目录管理 |
| Windows 接收服务 | Electron 主进程中的 Node.js HTTPS 服务 | 验证设备、流式接收照片、保存与确认 |
| Windows 索引 | 本地 SQLite，具体驱动开发时确定 | 去重、接收记录、历史照片查询 |
| 界面更新 | Electron IPC | 文件保存完成后通知界面，无需额外 WebSocket |

不需要 WebRTC、视频编解码器或云端媒体服务。上传原始 JPEG 或书写 PNG 字节，电脑显示时生成缩略图，原文件不进行二次压缩。

## 4. 首版界面

### Android

- 配对页：扫码入口、目标电脑名称、连接状态。
- 相机页：预览、拍照按钮、前后摄像头切换、最近一张照片缩略图。
- 书写页：白色画布和单行图标工具栏；画笔、带半透明触点圆面的橡皮擦、8 种颜色、1–32 级粗细（最粗约 1 cm）、撤销/重做、新建和同步；草稿自动保存。
- 传输状态：待传数量、正在上传、上传成功、失败原因和手动重试。
- 连接管理：断开连接、重新扫码、更换电脑。

默认使用后置摄像头。首版拍照过程中暂时禁用快门，文件写入成功后恢复；上传在独立队列中执行，不要求上一张上传完成才能拍下一张。

### Windows

- 未连接：配对二维码、电脑地址、服务状态、保存目录。
- 已连接：手机名称、连接状态、最新照片大图、历史缩略图。
- 图片操作：选择历史照片、适应窗口、缩放、打开所在文件夹。
- 默认新照片到达后自动展示；用户可以关闭“自动显示新照片”，避免查看历史时被打断。

## 5. 配对与连接

电脑服务生成配对二维码，内容包含协议版本、服务地址、电脑名称、短期一次性令牌、证书 SHA-256 指纹。建议令牌有效期为 5 分钟，过期后可以刷新二维码。

手机扫码后按二维码中的指纹校验电脑证书，再提交令牌完成配对。不可通过接受任意证书绕过校验。配对成功后签发设备凭据，由手机安全保存；电脑可以撤销配对。令牌和凭据不得写入普通日志。

同一时间只接受一部已配对手机。首版可以每次扫码建立会话，避免依赖局域网设备发现；保存配对身份不等于电脑 IP 地址永久不变。

电脑存在多个网卡时提供地址选择。连接故障需区分服务未启动、连接超时、证书不匹配、配对过期和未授权。说明 Windows 防火墙需要允许专用网络入站，以及访客 Wi-Fi 或客户端隔离可能阻止设备互通。

## 6. 照片传输与数据完整性

### 上传状态

```text
拍摄 → 本地保存 → 待上传 → 上传中 → 已确认
                         ↑         ↓
                         └── 失败待重试
```

拍摄时生成 photoId（UUID）。本地队列保存 photoId、目标电脑身份、文件路径、拍摄时间、字节数、SHA-256、状态和重试次数。更换电脑时，不自动把旧电脑的待传照片发送给新电脑，应由用户明确选择是否转移。

建议首版串行上传，失败后采用带上限的退避重试；授权失败、空间不足等需要用户处理的错误停止自动重试。首版失败后整张重新上传，不实现分片断点续传。

### 电脑接收步骤

1. 校验配对凭据、photoId、文件类型与大小限制。
2. 检查该设备的 photoId 是否已经接收成功。相同 ID、相同内容直接返回已有结果；相同 ID、不同内容返回冲突。
3. 流式写入目标目录中的临时文件，同时计算字节数和 SHA-256，避免整张照片常驻内存。
4. 校验大小、哈希和图片可解码性，关闭文件后在同一文件系统内重命名为正式文件。
5. 写入接收索引，再返回成功确认并通过 IPC 通知界面。

以“设备 ID + photoId”建立唯一约束，避免并发或重试造成重复保存。启动时清理未完成临时文件，并核对正式文件与索引，处理程序在重命名与索引提交之间退出的情况。

只有完整保存并建立接收记录后才能确认成功。电脑已经保存但手机没收到响应时，手机使用相同 photoId 重试，电脑返回同一接收结果。

### 文件管理

- 电脑建议路径：用户选择目录 / 接收日期 / 接收时间_photoId.jpg 或 `.png`。
- 服务器生成文件名，不使用手机提供的任意路径，避免覆盖和路径越界。
- 保存原始 JPEG 与 EXIF；预览正确应用 EXIF 方向，横拍和竖拍均需验收。
- 手机不自动删除未确认照片。首版也不自动删除已传照片，提供明确的本地清理入口。
- App 私有存储不等于系统相册，卸载 App 可能丢失本地照片；如需同步保存到系统相册，作为独立功能实现。
- 缩略图缓存与原始照片分离，清理缓存不删除原图。

## 7. 接口草案

所有接口带版本前缀 `/api/v1`。以下为开发约定草案，不代表现有接口。

| 接口 | 用途 | 关键内容 |
| --- | --- | --- |
| `POST /pair` | 扫码配对 | 一次性令牌、设备 ID、设备名称；返回设备凭据及电脑身份 |
| `GET /status` | 验证连接状态 | 需认证，返回服务是否可接收、协议版本 |
| `PUT /photos/{photoId}` | 幂等上传图片 | JPEG 或 PNG 请求体，附创建时间、长度与 SHA-256；返回接收记录 |
| `GET /photos/{photoId}/status` | 查询是否已保存 | 需认证，仅查询当前设备对应记录 |

统一返回结构化错误码，例如 `UNAUTHORIZED`、`PAIRING_EXPIRED`、`FILE_TOO_LARGE`、`DISK_FULL`、`CHECKSUM_MISMATCH`、`PHOTO_ID_CONFLICT`，供手机显示可理解的错误信息。

第一版建议单张上限 50 MiB；开发时根据目标手机样张确认，并明确显示超限错误。成功响应只返回记录 ID 和接收时间等必要信息，不暴露电脑完整文件路径。

## 8. 异常处理

| 情况 | 预期行为 |
| --- | --- |
| 手机断网、电脑关闭 | 手机保留队列，显示未连接，重新连接后继续 |
| 上传完成但确认丢失 | 相同 photoId 重试，电脑去重并返回已有结果 |
| 电脑目录不可写或磁盘满 | 不确认成功；手机保留照片，显示可操作错误 |
| 照片损坏或哈希不符 | 不进入图片列表，返回错误并保留手机原文件 |
| App 重启 | 从持久化队列恢复，将中断的上传重新排队 |
| 电脑软件重启 | 恢复历史索引，核对临时文件及未完成记录 |
| 用户拒绝相机权限 | 解释相机权限用途，提供重试或系统设置入口 |
| 手机存储不足 | 明确提示拍摄未成功，不创建虚假的待传记录 |

## 9. 开发顺序

1. **最小链路**：Android 拍照并本地保存，Windows 接收单张 JPEG 并显示。开发测试可手动输入地址，但认证不能在交付版本中省略。
2. **正常使用流程**：二维码配对、自动上传、保存目录选择、最新大图与历史缩略图。
3. **可靠性**：持久化队列、去重、超时重试、哈希校验、重启恢复、磁盘及权限错误处理。
4. **交付验证**：打包 Android APK 和 Windows 安装包，在无开发环境的电脑上完成安装与端到端测试。

2026-09-13 开发进展：Windows 接收服务、配对、照片保存与展示已实现，并以模拟客户端验证；Android 已实现扫码、拍照、持久化队列和自动上传，真机端到端验收尚未完成。

## 10. 首版验收标准

- 手机拍照后，电脑自动保存并展示，无需逐张手动导入。
- 手机书写后点击同步，电脑保存原始 PNG、自动展示，并可继续使用缩放和截图功能。
- 手机本地原图与电脑文件 SHA-256 一致，图片方向正确。
- 连续拍摄 20 张，每张均接收一次，无丢失或重复。
- 网络中断后拍摄的照片进入队列，恢复连接后自动发送。
- 上传过程中关闭并重开 App，待传记录可以恢复。
- 模拟确认响应丢失、电脑重启、磁盘满、目录不可写，状态与结果符合异常处理约定。
- 未配对设备无法上传；过期二维码不能继续配对。
- 在约定的 Wi-Fi 测试环境下，以约 5 MB JPEG 从拍摄完成到电脑展示 3 秒内作为初始体验目标，记录设备、网络、样本数量及 P50/P95；该目标尚未验证。
- Windows 界面只通过受限 IPC 调用文件功能，开启上下文隔离、关闭渲染进程 Node 集成。

## 11. 实现参考

- [Android CameraX 架构](https://developer.android.com/media/camera/camerax/architecture)
- [Electron 安全建议](https://www.electronjs.org/docs/latest/tutorial/security)
- [Electron 上下文隔离](https://www.electronjs.org/docs/latest/tutorial/context-isolation)

具体 SDK、二维码组件、SQLite 驱动及打包工具版本在开发启动时核实并锁定。
