# MobileVision 当前状态

更新：2026-09-13

## 2026-09-23 手机书写模式

Android 底部新增独立“书写”页，提供白色画布、画笔、橡皮擦、颜色与粗细选择、撤销、重做和立即清屏。清屏按钮直接清空当前草稿，不再弹确认框。笔迹完成后自动写入本地草稿；点击“同步”会生成最高 1600×2800 的无损 PNG 快照，进入现有持久队列，电脑离线时保留并自动重试。每次同步生成独立图片，草稿继续保留和编辑。

书写现为应用默认第一页和底部导航第一项。应用默认锁定竖屏，书写页提供独立旋屏按钮，不依赖手机全局自动旋转。所有书写按钮以半透明工具组浮在画布上；横屏时进入沉浸式全屏布局，画笔、橡皮、撤销和重做排列在右侧，颜色、粗细、清屏、同步和恢复竖屏排列在右上角，连接栏、底部导航、状态文字和 Android 系统栏隐藏。

书写工具进一步压缩为单行图标：画笔、橡皮、颜色、粗细、撤销、重做、清屏和同步完整显示在同一行。颜色使用无额外按钮的窄弹窗和小色点；粗细使用仅含圆形预览与滑轴的极简弹窗，支持 1–32 级，第 32 级依据设备物理 DPI 计算为约 1 cm，点击弹窗外即可关闭。橡皮拖动时仅显示与擦除直径一致的单层半透明圆面，松手后隐藏。当前画笔/橡皮状态高亮，所有图标保留无障碍说明。

Windows 接收端现同时严格校验 JPEG 与 PNG 的请求类型、内容格式、大小和 SHA-256，按实际格式保存原图，缩略图、预览、截图和删除继续复用现有流程。Windows 类型检查及 5 项接收/截图测试通过，其中接收测试新增 PNG 原始字节与扩展名校验。

Windows 主预览永久跟随最新照片，已彻底移除“自动显示新照片”选项；点击历史缩略图使用临时查看层，不改变主预览。置顶悬浮窗及其拖动、框选和独立窗口接口已经删除。

Android 书写页保留“同步并插入”：把 PNG 上传、电脑确认和焦点输入框粘贴合为一个持久任务；断线重试保留粘贴意图，Windows 在首次保存后把整张图片写入剪贴板并向当前焦点输入框发送粘贴命令，重复上传不会重复插入。

PHP110 真机已验证触控书写、应用强制停止后的草稿恢复、新建清空，以及向打包后的 Windows 客户端同步 1600×2055 PNG。电脑保存的 PNG 与手机画布一致，接收记录为 ready、尺寸正确；Windows 安装包已重建，打包程序冒烟退出码为 0。测试笔迹同步后已清空手机草稿，电脑照片目录保留一张实际链路验收图。

## 2026-09-23 Android 相册导入

拍摄页新增“导入相册”。用户通过 Android 系统图片选择器选择一张现有图片后，App 将其写入与拍照相同的本地持久队列并自动传到已配对电脑；电脑离线时保留并在恢复连接后重试。JPEG 保留原文件，其他系统可解码格式转换为高质量 JPEG，不需要读取整个相册的权限。

Debug 应用 APK、仪器测试 APK 构建成功，6 项 JVM 单元测试通过；仪器测试源码也已编译，并检查配对后“导入相册”入口可见且可用。新 APK 已保留数据安装到 PHP110，真机确认按钮显示且能打开 Android 系统照片选择器；尚未代替用户选择私人照片，实际图片转换及上传仍需一次真机交互验收。

拍摄操作区改为图标布局：80dp 圆形快门居中作为主操作，52dp 相册导入和镜头切换按钮分列两侧。三个图标均保留无障碍文字说明，操作行高度同步缩小，为取景区留出更多空间。新版已安装到 PHP110 并完成真机视觉核对。

## 本次已修复

- 手机照片时间按设备时区显示，支持跨日期和夏令时。
- 快门固定底部，拍摄与照片记录分开；历史每页 20 张，清理后返回第一页。
- 电脑可复制连接信息；已配对时可显示更新地址二维码。手机校验原电脑 ID、证书指纹并通过认证后才更新地址，原凭据和待传照片保留。
- 配对前加密保存候选凭据，电脑接受配对后即使响应丢失，手机也能用认证状态请求恢复配对。兼容旧客户端由电脑生成凭据。
- Windows 可导出诊断日志，持久保存最近 200 条事件代码，不收集凭据、照片、目录或设备名。
- 提供 Windows NSIS 安装包，可选安装目录，按当前用户安装，卸载保留应用数据。
- 真机发现的恢复连接后残留离线文字已修复。

## 本轮验证

- Windows TypeScript 检查通过；3 项接收集成测试通过，包括凭据恢复与日志脱敏。
- Android Debug APK 构建成功；4 项单元测试通过（时间 3 项、重试 1 项）。
- 两项跨端仪器测试通过：主动丢弃首次配对成功响应后恢复、CameraX 模拟器拍照、离线队列、进程重启续传、重复上传去重、不同端口地址更新且凭据不变、错误电脑 ID/指纹拒绝、固定快门与照片页切换。
- 两张照片原图 SHA-256 与电脑文件一致，Windows 实际窗口已展示照片与缩略图。
- 真实 PHP110 手机已保留现有数据更新安装、成功启动；原配对通过 wlan0 局域网恢复，adb reverse 列表为空。本次没有在真机自动拍摄环境照片。
- Windows 安装包构建成功，约 119 MB，Authenticode 状态为 NotSigned；未在另一台电脑完成安装/卸载验证。

证据：[跨端报告](windows/test-results/android-e2e/report.json)、[配对与离线测试](windows/test-results/android-e2e/captureAndQueueOffline.txt)、[重启与重连测试](windows/test-results/android-e2e/resumeAfterProcessRestart.txt)、[Windows 截图](windows/test-results/android-e2e/windows-received.png)。本轮跨端回归使用开发态 Electron；安装包包含相同生产逻辑。最后一次文字提示修复另经构建、单元测试和真机界面验证。

## 尚需现场验收

真实手机对电脑屏幕光学扫码、前后摄像头照片及 EXIF 方向、拒绝权限后恢复、连续拍摄、切后台与重开、真实存储不足。现有 Wi-Fi 连接通过不代表所有路由器/防火墙环境均通过。

正式签名、正式应用图标/版本资源、证书到期更新仍属于后续发布完善项。

## 可运行文件

- [Windows 安装包](windows/release/MobileVision%20Setup%200.1.0.exe)
- [Windows 程序](windows/release/win-unpacked/MobileVision.exe)（须保留整个目录）
- [Android APK](android/app/build/outputs/apk/debug/MobileVision-Android-0.2.0-debug.apk)

## 拍摄界面调整

拍摄页顶部仅保留电脑名称、连接状态和设置入口；扫码、粘贴配对信息、重试、断开移入连接设置弹窗。取景区随剩余屏幕高度伸展，快门固定在底部。拍摄/照片记录改为带图标和选中高亮的 Material 导航栏，待传数量保留为一行提示。

## Windows 多选删除

在最近接收区域点击“多选删除”，逐张勾选或全选当前列表，再点击“删除所选”。系统弹窗确认后永久删除所选电脑原图、缩略图和记录，不进入回收站；手机副本保持不变。最多选当前列表 500 张。清理失败可重试，中断删除在重启后继续。4 项接收集成测试通过；软件渲染模式下桌面冒烟验证了多选状态与删除后预览更新。测试只使用隔离样图。

打包后的 Windows 程序已通过同一多选/删除界面冒烟检查，退出码 0；安装包已同步更新。

## 2026-09-23 截图交互更新

照片区域右键直接进入截图模式。框外拖动可重新框选；在已有截图框内按住左键拖动时，只移动截图框并限制在原图范围内，照片内容不会移动。双击截图框会复制按原图像素裁切的 PNG 到系统剪贴板，并立即退出截图模式。保存图标仍用于另存 PNG 文件。

TypeScript 检查及 5 项单元/接收集成测试通过；Electron 操作验收覆盖右键进入、框选、移动截图框、照片保持不动、双击复制、退出截图模式和保存 PNG。

截图视觉反馈已细化：右键进入截图模式后立即在照片范围显示半透明蒙版，无需先按下鼠标；截图框出现后，框内鼠标显示可拖动手势，按住时显示正在拖动手势，框外仍为十字框选手势。Electron 操作验收检查了蒙版和鼠标样式。

截图模式的右键行为继续统一：无截图框时可在整张蒙版上右键退出；已有截图框时可在框外蒙版区域右键退出。截图框内始终显示移动光标，按下及拖动时不再切换样式；框外仍为十字光标。完整 Electron 流程验收通过。

截图蒙版已统一为照片显示组件的一层蒙版，不再按实际照片边界形成第二块视觉区域。截图模式下右键任意位置均退出，包括截图框内部。截图框静止时显示点击手形光标，拖动过程中显示抓取手形，框外保持十字光标。Electron 验收核对了蒙版尺寸、选区内右键退出及光标状态切换。

框选开始时的蒙版闪烁已消除：鼠标按下创建选区后，整层蒙版立即由选区外遮罩接替，两层不会同时显示。截图框四角新增圆形缩放点，可从任意角调整宽高并限制在原图范围内。Electron 验收检查了蒙版交接、四个缩放点、缩放光标及实际尺寸变化。

修复截图框拖动时光标回退为十字的问题：截图框按下后由照片容器接管指针，容器现在会在整个拖动期间保持抓取手形，松开后恢复截图框的点击手形。Electron 真实输入验收覆盖了按下与移动两个阶段。

取景框进一步改为按 CameraX 实际裁剪区域和旋转信息计算宽高比，保持完整预览，不拉伸画面；窗口下紧接传输状态与快门，页面导航仍在底部。

## 2026-09-23 桌面图片交互与截图

预览区支持围绕鼠标位置滚轮缩放（最高 800%）、左键拖动平移、双击适应窗口。点击“截图”后拖动框选，再点“保存截图”另存为 PNG；Esc 或“取消截图”退出。按经过 EXIF 方向纠正的原图像素导出，选区边缘向外取整；原照片保持不变。开始滚轮、拖动或截图会关闭自动显示新照片，防止操作时切图。截图为独立文件，不加入接收历史。

类型检查和 5 项单元/集成测试通过；真实 Electron 输入验收覆盖滚轮、拖动、框选、保存、删除与预览更新。截图测试覆盖旋转照片、越界拒绝、输出像素与原文件保持不变。

## 2026-09-23 截图操作调整

进入截图模式后拖动框选。双击选区直接将 PNG 图片复制到系统剪贴板；点击保存图标才打开另存为对话框并生成 PNG 文件。双击选区外不会复制。两条路径共用原图像素裁切与方向修正，原照片不变。
